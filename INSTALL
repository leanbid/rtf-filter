Ensure your system has build-essential Debian package installed, and automake too.

Within the rft-filter directory issue these commands:

./configure
make all
sudo make install

The program will be installed as rtffilter.










